<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RivoApplication"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:Microsoft.UI.Xaml.Controls"
    x:Class="RivoApplication.BatteryPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!--StackPanel의 경우 단일 행에 요소들을 넣기때문에 한 행에 하나의 element만 들어감-->
        <TextBlock x:Name="status" HorizontalAlignment="Left" Text="연결 상태 :" FontSize="24" Margin="15,5,0,678" FontWeight="Bold"/>
        <TextBlock x:Name="battery" HorizontalAlignment="Left"  Text="Battery :" FontSize="24" Margin="23,46,0,638" FontWeight="Bold"/>
        <ProgressBar HorizontalAlignment="Left" Height="30" Margin="134,50,0,0" VerticalAlignment="Top" Width="200"/>
        <!--ProgressBar의 IsIndeterminate이 false이면 value에 따라 bar를 채운다.-->
        <!--<ProgressBar x:Name="Battery" Height="30" 
                     Margin="141,36,863,0" VerticalAlignment="Top" Foreground="#FF61E623" ValueChanged="Battery_ValueChanged"/>-->

    </Grid>
</Page>
